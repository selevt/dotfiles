" display configuration
"

colorscheme Tomorrow-Night

" break lines when longer than screen
set wrap

" show a colored column to restrict too long lines
if version >= 703
    set colorcolumn=105
endif

" special list characters
set listchars=tab:¿\ ,eol:¬,trail:·
